import status from '../constants/status';
import headers from '../constants/headers';

export default{
    issues : [],
    comments : [],
    status : status,
    users : [],
    headers : headers
};